package me.intuit.cat.data.local

/*

class AppDatabaseService @Inject constructor(val appDatabase: AppDatabase) : DatabaseService {

    override fun getBreeds(): Flow<List<BreedEntity>> {
        return appDatabase.breedDao().breeds()//breedDao().getAll()
    }

    override fun deleteAllAndInsertAll(breeds: List<BreedEntity>) {
        appDatabase.breedDao().deleteAllAndInsertAll(breeds)
    }

}
*/
